'use strict';

class PlainParser {
  parse(tokens) {
    const node = null;  ///

    return node;
  }

  static fromNothing() {
    const plainParser = new PlainParser();

    return plainParser;
  }
}

module.exports = PlainParser;
