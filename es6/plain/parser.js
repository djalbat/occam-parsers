'use strict';

class PlainParser {
  nodeFromLines(lines) {
    const node = null;  ///
    
    return node;
  }
  
  static fromNothing() {
    const plainParser = new PlainParser();

    return plainParser;
  }
}

module.exports = PlainParser;
