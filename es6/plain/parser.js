'use strict';

class PlainParser {
  nodeFromTokens(tokens) {
    const node = null;  ///
    
    return node;
  }
  
  static fromNothing() {
    const plainParser = new PlainParser();

    return plainParser;
  }
}

module.exports = PlainParser;
